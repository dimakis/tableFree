// import Table from './tableClass'

export default class TableTimeSlots {
    constructor(time) {
        this.time = time;
        this.bookedBy = "";
        this.isBooked = false;
    }

    // public setTime(time)
}